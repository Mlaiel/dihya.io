// Exemple avancé validateur input (Node.js)
function validateInput(input) {
  // Logique de validation avancée
  return typeof input === 'string' && input.length > 0;
}

module.exports = { validateInput };
