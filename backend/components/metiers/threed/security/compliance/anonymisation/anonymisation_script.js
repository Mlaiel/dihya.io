// anonymisation_script.js – Anonymisation RGPD

function anonymiseData(path) {
  // Exemple : suppression des champs sensibles
  console.log(`[ANONYMISATION] Données anonymisées dans ${path}`);
}

module.exports = { anonymiseData };
