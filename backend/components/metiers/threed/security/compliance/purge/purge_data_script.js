// purge_data_script.js – Purge RGPD

function purgeSensitiveData(target) {
  // Exemple : suppression sécurisée de fichiers sensibles
  console.log(`[OK] Purge RGPD terminée dans ${target}`);
}

module.exports = { purgeSensitiveData };
