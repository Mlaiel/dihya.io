// index.js - Exécution centralisée des samples RBAC
const fixtures = require('./fixtures');
const mocks = require('./mocks');
const templates = require('./templates');
const sampleUsage = require('./sample_usage');
const sampleData = require('./sample_rbac_data.json');
module.exports = { fixtures, mocks, templates, sampleUsage, sampleData };
