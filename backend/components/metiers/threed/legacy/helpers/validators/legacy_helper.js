// legacy_helper.js – Helper legacy avancé pour la validation métier (clé en main)
function validateLegacy(data) {
  // Valide les données legacy pour le métier (stub à adapter selon logique métier)
  return !!data;
}

module.exports = { validateLegacy };
